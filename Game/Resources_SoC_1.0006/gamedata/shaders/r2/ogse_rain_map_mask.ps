#include "common.h"

#define EPS	(0.9f/255.f)
float4 	main		( p_screen I )	: COLOR
{
  // Sample the fat framebuffer:
  float4 NH		= get_full_normal(I.tc0);//tex2D 		(s_normal,   I.tc0); 
  float	L 		= dot	(Ldynamic_dir, NH.xyz) + EPS/*)*/;
  return  		float4			(L,L,L,L);
}
