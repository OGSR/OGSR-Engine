#include "common.h"
#include "infrared.h"

float4 main(p_screen I) : SV_Target
{
    float3 image = s_image.Sample(smp_rtlinear, I.tc0).xyz;
    image = infrared(gbuffer_load_normal(I.tc0), image, gbuffer_load_depth(I.tc0), I.tc0);
    return float4(image, 1.0);
}
